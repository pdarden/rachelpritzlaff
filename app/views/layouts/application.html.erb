<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="google-site-verification" content=<%= ENV['Google_Verification'] %> />
    <meta name="msvalidate.01" content=<%= ENV['Bing_Verification'] %> />
    <%= metamagic title: "Rachel Pritzlaff | Time Based Art", description: "Rachel Pritzlaff creates work under the title of Rachel Pritzlaff/Time Based Art that overhauls the ordinary with wit, precision, and absurd tendencies.", keywords: ["Rachel Pritzlaff", "Time Based Art", "modern dance", "dance", "modern dance", "NYC", "New York City", "comtemporary", "performance art", "performance", "choreography", "post-modern"] %>

    <link href="http://fonts.googleapis.com/css?family=Cardo:400|Josefin+Slab:400" rel="stylesheet" type="text/css">
    <link href='http://fonts.googleapis.com/css?family=Poiret+One' rel='stylesheet' type='text/css'>
    <link href="//cdn-images.mailchimp.com/embedcode/classic-081711.css" rel="stylesheet" type="text/css">
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag :modernizr %>
    <%= csrf_meta_tags %>

  </head>

  <body class="body clearfix">
    <header class="clearfix">
    <%= link_to 'Rachel Pritzlaff / Time Based Art', root_path %>
    </header>

    <%= render 'shared/nav' %>

    <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
        <div class="alert alert-<%= name == :success ? "success" : (name == :notice ? "info" : "danger") %>">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        </div>
      <% end %>
    <% end %>

    <div class="main-container">
      <%= yield %>
    </div>

    <%= javascript_include_tag "application" %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', <%= ENV['Google_Analytics'] %>, 'rachelpritzlaff.com');
      ga('send', 'pageview');

    </script>

  </body>
</html>
